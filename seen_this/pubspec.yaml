name: seen_this
description: "A Flutter app for organizing shared content."
publish_to: 'none'

version: 1.0.0-beta+2

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  # UI & Icons
  cupertino_icons: ^1.0.8

  # State Management
  provider: ^6.1.0

  # Storage
  shared_preferences: ^2.2.0
  path_provider: ^2.1.0
  path: ^1.9.0
  sqflite: ^2.3.0

  # Utilities
  intl: ^0.19.0
  uuid: ^4.0.0

  # Sharing
  share_plus: ^7.2.0
  url_launcher: ^6.2.0

  # Link Preview & Metadata
  http: ^1.1.0
  html: ^0.15.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1
  sqflite_common_ffi: ^2.3.0

flutter:
  uses-material-design: true
  assets:
    - assets/seen_this_logo.png
  

